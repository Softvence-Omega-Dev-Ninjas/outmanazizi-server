// Base schema file - generator and datasource are in _schema.composed.prisma
// This file is used by the compose script to generate the final schema


generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
