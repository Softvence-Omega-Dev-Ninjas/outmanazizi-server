// Generator and datasource definitions for composed schema
// These will be used in the final _schema.composed.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
