model Dispute {
  id                String      @id @default(uuid())
  bidId             String
  bid               Bid         @relation(fields: [id], references: [id])
  details         String   
  isSolved            String
  pictures          String[]?
  createdAt         DateTime    @default(now())
  updatedAt        DateTime    @updatedAt
}